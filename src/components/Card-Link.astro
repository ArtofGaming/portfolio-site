---
interface Props {
	title: string;
	body: string;
	href: string;
	image: string;
	pubDate: Date;
	link: string;
}

const { href, title, body, image, link} = Astro.props;
---
<div style="padding-left:0; padding-right:0; min-height:0; overflow:hidden; border-radius:5px;">
	<li class="link-card">
		<a href={link}>
			<div class="container">
				<img class="card-img" src={image} alt="project image">
				</img>
				<div style="display:grid; align-self: bottom;height:auto; width:100%;">
						<h2>
							{title}
						</h2>
					<p style=" padding-bottom:1ch">
						{body}
					</p>
				</div>
			</div>
		</a>
	</li>
	
</div>
<style>
	.link-card {
		padding-top: 0em;
		min-height:0;
		max-height:17rem;
		height:auto;
		width:100%;
		border-radius: 1px;
		opacity: .7;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		text-overflow:hidden;
		align-self: center;
	}
	.link-card > a {
		text-decoration: none;
		align-self: center;
		
		min-height:0;
		max-height:17rem;
		height:auto;
		line-height: 1.4;
		padding-top: calc(1rem);
		width: 100%;
		color: white;
		opacity: 1;
		text-overflow:hidden;
	}
	h2 {
		text-align: center;
		font-size: 1.25rem;
		color: rgb(228, 157, 126);
		transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		margin-top:0em;
		padding-right: 2em;
	}
	p {
		text-align: center;
		width:100%;
		height:auto;
		margin-top: 0;
		margin-bottom: 0;
		padding-right: 2em;
	}
	.link-card:is(:hover, :focus-within) h2 {
		color: rgb(var(--link-color));
	}
</style>
